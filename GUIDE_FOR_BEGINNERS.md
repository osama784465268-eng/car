# 🚗 الدليل الشامل والسهل لتشغيل معرض السيارات على XAMPP
> **موجّه للطلاب والمبتدئين:** هذا الدليل مكتوب بأسلوب بسيط ومُبسط خطوة بخطوة، حتى لو لم تكن لديك أي خبرة سابقة في البرمجة!

---

## 📋 فهرس المحتويات
1. [ما هو هذا المشروع وماذا نحتاج لتشغيله؟](#1-ما-هو-هذا-المشروع-وماذا-نحتاج-لتشغيله)
2. [الخطوة الأولى: تثبيت وتشغيل برنامج XAMPP](#2-الخطوة-الأولى-تثبيت-وتشغيل-برنامج-xampp)
3. [الخطوة الثانية: نقل مجلد المشروع إلى المكان الصحيح](#3-الخطوة-الثانية-نقل-مجلد-المشروع-إلى-المكان-الصحيح)
4. [الخطوة الثالثة: إنشاء قاعدة البيانات في phpMyAdmin](#4-الخطوة-الثالثة-إنشاء-قاعدة-البيانات-في-phpmyadmin)
5. [الخطوة الرابعة: استيراد جدول قاعدة البيانات (`setup.sql`)](#5-الخطوة-الرابعة-استيراد-جدول-قاعدة-البيانات-setupsql)
6. [الخطوة الخامسة: تفعيل محرك SQLite في XAMPP](#6-الخطوة-الخامسة-تفعيل-محرك-sqlite-في-xampp)
7. [الخطوة السادسة: نقل السيارات والبيانات تلقائياً (سكربت الهجرة)](#7-الخطوة-السادسة-نقل-السيارات-والبيانات-تلقائياً-سكربت-الهجرة)
8. [الخطوة السابعة: فتح الموقع واستخدامه!](#8-الخطوة-السابعة-فتح-الموقع-واستخدامه)
9. [حل المشاكل الشائعة (إذا واجهتك مشكلة)](#9-حل-المشاكل-الشائعة-إذا-واجهتك-مشكلة)

---

## 1. ما هو هذا المشروع وماذا نحتاج لتشغيله؟

### ما هو هذا المشروع؟
هذا المشروع هو **موقع متكامل لمعرض سيارات** يحتوي على:
- عرض السيارات مع صورها وتفاصيلها.
- إمكانية البحث والفلترة حسب الماركة، السعر، المدينة، نوع الوقود، والسنة.
- لوحة إحصائيات ورسوم بيانية تفاعلية.
- إمكانية إضافة سيارة جديدة، تعديل بيانات سيارة، أو حذفها.

### ماذا نحتاج لتشغيله على جهازك؟
نحتاج إلى برنامج واحد فقط اسمه **XAMPP** (سيرفر محلي مجاني يتيح لجهازك تشغيل مواقع PHP وقواعد البيانات).

---

## 2. الخطوة الأولى: تثبيت وتشغيل برنامج XAMPP

1. **تحميل البرنامج**: إذا لم يكن لديك برنامج XAMPP، قم بتحميله من الموقع الرسمي:
   - ابحث في قوقل عن **XAMPP** أو حمل من هذا الرابط: `https://www.apachefriends.org`
2. **التثبيت**: افتح الملف المحمل واضغط `Next` -> `Next` -> `Finish` (تثبيت عادي جداً مثل أي برنامج).
3. **تشغيل XAMPP**:
   - افتح برنامج **XAMPP Control Panel**.
   - ستظهر لك لوحة فيها عدة خيارات.
   - اضغط على زر **Start** بجانب **Apache** (ليتحول لونه للأخضر).
   - اضغط على زر **Start** بجانب **MySQL** (ليتحول لونه للأخضر).

> 💡 **ملاحظة مهمة:** يجب أن يكون **Apache** و **MySQL** يعملان باللون الأخضر طوال فترة استخدامك للموقع.

---

## 3. الخطوة الثانية: نقل مجلد المشروع إلى المكان الصحيح

برنامج XAMPP لا يقرأ المواقع إلا إذا كانت داخل مجلد خاص اسمه `htdocs`.

1. افتح **المستكشف (File Explorer)** واذهب إلى المسار التالي (غالباً في القرص C):
   ```text
   C:\xampp\htdocs\
   ```
2. قم بنسخ مجلد مشروعنا اسمه `newcar` ولصقه داخل `htdocs`.
3. يجب أن يصبح المسار النهائي كالتالي:
   ```text
   C:\xampp\htdocs\newcar\
   ```

> ⚠️ **تأكد:** عند فتح `C:\xampp\htdocs\newcar` يجب أن ترى ملف `index.php` ومجلدات مثل `api` و `css` و `js` مباشرة.

---

## 4. الخطوة الثالثة: إنشاء قاعدة البيانات في phpMyAdmin

الآن نحتاج لإنشاء مَخزن للبيانات (قاعدة بيانات) لحفظ معلومات السيارات.

1. افتح متصفح الإنترنت (Google Chrome أو Edge).
2. اكتب في شريط العنوان الرابط التالي ثم اضغط Enter:
   ```url
   http://localhost/phpmyadmin/
   ```
3. ستفتح لك لوحة إدارة قواعد البيانات باللغة العربية أو الإنجليزية.
4. **إنشاء القاعدة**:
   - في القائمة الجانبية اليسرى، اضغط على **جديد** (أو **New**).
   - ستظهر لك خانة لكتابة اسم قاعدة البيانات.
   - اكتب الاسم **بالضبط** كما هو موضح بالأسفل (بدون مسافات أو زيادات):
     ```text
     newcar_db
     ```
   - في الخانة المجاورة لترميز اللغة، اختر: `utf8mb4_unicode_ci` (ليدعم اللغة العربية بشكل ممتاز).
   - اضغط على زر **إنشاء** (أو **Create**).

🎉 ممتاز! الآن أصبحت لديك قاعدة بيانات فارغة باسم `newcar_db`.

---

## 5. الخطوة الرابعة: استيراد جدول قاعدة البيانات (`setup.sql`)

الآن سنقوم بإنشاء هيكل الجداول داخل قاعدة البيانات الجديدة `newcar_db`.

1. تأكد أنك ما زلت داخل صفحة `phpMyAdmin` وأنك ضغطت على اسم قاعدة البيانات `newcar_db` من القائمة اليسرى.
2. من الشريط العلوي، اضغط على تبويب **استيراد** (أو **Import**).
3. اضغط على زر **اختيار ملف** (أو **Choose File**).
4. اذهب إلى مجلد المشروع على جهازك واختر الملف التالي:
   ```text
   C:\xampp\htdocs\newcar\sql\setup.sql
   ```
5. انزل إلى أسفل الصفحة واضغط على زر **استيراد** (أو **Go** / **Import**).
6. ستظهر لك رسالة خضراء تخبرك: *"تم تنفيذ الاستيراد بنجاح"* (Import has been successfully finished).

---

## 6. الخطوة الخامسة: تفعيل محرك SQLite في XAMPP

نحتاج هذه الخطوة **مرة واحدة فقط** لنتمكن من نقل السيارات الموجودة مسبقاً في المشروع إلى قاعدة البيانات الجديدة.

1. افتح لوحة **XAMPP Control Panel**.
2. ابحث عن السطر الخاص بـ **Apache**، واضغط على زر **Config** الموجود بجانبه.
3. اختر من القائمة الملف الأول: `php.ini`.
4. سيفتح لك الملف في دفتر الملاحظات (Notepad).
5. اضغط من الكيبورد على `Ctrl + F` للبحث، واكتب:
   ```text
   pdo_sqlite
   ```
6. ستجد السطر مكتوباً بالشكل التالي (مع فاصلة منقوطة `;` في بدايته):
   ```ini
   ;extension=pdo_sqlite
   ```
7. قم بإلغاء الفاصلة المنقوطة `;` من بداية السطر ليصبح هكذا بالضبط:
   ```ini
   extension=pdo_sqlite
   ```
8. احفظ الملف (`Ctrl + S`) ثم أغلق دفتر الملاحظات.
9. في لوحة XAMPP، اضغط على **Stop** بجانب **Apache** ثم اضغط **Start** مرة أخرى لإعادة تشغيله بالخيار الجديد.

---

## 7. الخطوة السادسة: نقل السيارات والبيانات تلقائياً (سكربت الهجرة)

لقد أعددنا لك سكربت ذكي يقوم بنقل مئات السيارات الجاهزة إلى قاعدة بياناتك الجديدة تلقائياً بدون تعب!

1. افتح متصفحك واكتب الرابط التالي:
   ```url
   http://localhost/newcar/migrate/migrate_sqlite_to_mysql.php
   ```
2. اضغط Enter.
3. ستظهر لك صفحة جميلة تخبرك:
   - 🟢 *"تم ترحيل البيانات بنجاح!"*
   - 🟢 *"عدد السيارات التي تم نقلها: 350 سيارة"*

---

## 8. الخطوة السابعة: فتح الموقع واستخدامه!

أنت الآن جاهز تماماً! كل شيء يعمل بشكل سليم 100%.

1. افتح المتصفح واكتب الرابط الرئيسي للموقع:
   ```url
   http://localhost/newcar/
   ```
2. مبروك! 🥳 سيظهر لك موقع معرض السيارات كاملاً مع:
   - قائمة السيارات مع صورها وتفاصيلها.
   - خيارات الفلترة المتقدمة والبحث الحركي.
   - قسم الإحصائيات والرسوم البيانية.
   - إمكانية إضافة وإدارة السيارات.

---

## 9. حل المشاكل الشائعة (إذا واجهتك مشكلة)

| المشكلة | السبب | الحل |
| :--- | :--- | :--- |
| الصفحة لا تفتح وتظهر `404 Not Found` | لم تقم بنقل المجلد إلى المكان الصحيح. | تأكد أن مجلد المشروع موجود داخل `C:\xampp\htdocs\newcar`. |
| خطأ `Database connection failed` | قاعدة البيانات غير موجودة أو اسمها خاطئ. | تأكد أنك أنشأت قاعدة البيانات في phpMyAdmin باسم `newcar_db` بالضبط. |
| خطأ عند تشغيل سكربت الهجرة `PDOException: driver not found` | لم تقم بتفعيل `pdo_sqlite` في `php.ini`. | راجع **الخطوة الخامسة** وحذف الفاصلة المنقوطة `;` ثم أعد تشغيل Apache. |
| Apache لا يعمل باللون الأخضر (Port in use) | هناك برنامج آخر يستخدم منفذ 80 (مثل Skype أو Zoom). | أغلق البرامج الأخرى، أو غير المنفذ من إعدادات XAMPP. |

---

### 🎉 نتمنى لكم التوفيق في دراستكم واستعراض المشروع! 🚗💨
